services:
  docs:
    image: oscar_web_app_local_docs
    container_name: oscar_web_app_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./oscar_web_app:/app/oscar_web_app:z
    ports:
      - '9000:9000'
    command: /start-docs
